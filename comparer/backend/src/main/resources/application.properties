# Base de donn\u00e9es MySQL
spring.datasource.url=jdbc:mysql://localhost:3306/smartintern_db?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.open-in-view=false

# JWT
app.jwt.secret=smartintern_super_secret_key_2024_change_in_prod
app.jwt.expiration=86400000

# Port serveur
server.port=8081

# Mail SMTP Gmail
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=smartinternainoreply@gmail.com
spring.mail.password=mlsp qzxm wgdg mzci
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.writetimeout=5000

# Exp\u00e9diteur
app.mail.from=smartinternainoreply@gmail.com
app.mail.from-name=SmartIntern AI

# Upload fichiers
spring.servlet.multipart.enabled=true
spring.servlet.multipart.max-file-size=5MB
spring.servlet.multipart.max-request-size=5MB

# Dossier de stockage des CV
app.upload.cv-dir=uploads/cv

logging.level.org.springframework.security=DEBUG
logging.level.com.smartintern.backend.security=DEBUG
logging.level.org.hibernate.SQL=DEBUG
logging.level.root=INFO

logging.file.name=logs/smartintern.log
logging.level.root=INFO

# application.properties additions:
 
# WebSocket
spring.websocket.max-text-message-size=524288
spring.websocket.max-binary-message-size=524288
 
# Upload fichiers
spring.servlet.multipart.enabled=true
spring.servlet.multipart.max-file-size=50MB
spring.servlet.multipart.max-request-size=50MB
 
# Dossier d'upload (cr\u00e9er \u00e0 la racine du projet)
app.upload.dir=uploads/messages/
 
# =========================
# FICHIER LOG
# =========================
logging.file.name=logs/smartintern.log

# Taille max du fichier
logging.logback.rollingpolicy.max-file-size=10MB

# Nombre de fichiers conserv\u00e9s
logging.logback.rollingpolicy.max-history=30
# SECURITY
logging.level.org.springframework.security=DEBUG

# WEBSOCKET
logging.level.org.springframework.web.socket=DEBUG
logging.level.org.springframework.messaging=DEBUG

# HIBERNATE
logging.level.org.hibernate.SQL=DEBUG

# VOTRE APP
logging.level.com.smartintern=DEBUG